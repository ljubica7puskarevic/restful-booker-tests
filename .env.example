# Optional overrides. Copy this file to .env (git-ignored) and uncomment a line
# to change it. When a value is unset the suite uses its built-in default: the
# base URL from playwright.config.ts and the public docs credentials from
# tests/helpers/api.ts.

# BASE_URL=https://your-own-instance.example.com
# USERNAME=
# PASSWORD=